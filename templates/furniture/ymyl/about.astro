---
import BaseLayout from '@platform/core/src/layouts/BaseLayout.astro'
import { getPersona, getSiteConfig } from '@platform/core/src/lib/config'

const persona = getPersona()
const cfg = getSiteConfig()
---

<BaseLayout
  title={`About ${persona.name_used} — ${cfg.site.brand_name}`}
  description={`Meet ${persona.name_formal}, the person behind ${cfg.site.brand_name}. ${persona.bio_short}`}
>
  <div class="container" style="padding: 3rem 1.5rem;">
    <article class="about-page">
      <div class="about-page__intro">
        <img
          src={persona.photo_about}
          alt={persona.name_formal}
          class="about-page__photo"
          width="200"
          height="200"
          fetchpriority="high"
          decoding="async"
        />
        <div>
          <h1>About {persona.name_used}</h1>
          <p class="about-page__location">📍 {persona.location}</p>
          <p>{persona.bio_short}</p>
        </div>
      </div>

      <div class="about-page__body" set:html={persona.bio_full.replace(/\n\n/g, '</p><p>')} />

      <div class="about-page__disclaimer">
        <h2>Editorial approach</h2>
        <p>{cfg.site.brand_name} provides editorial research and commentary on {cfg.site.niche} products. Recommendations are based on verified owner research, manufacturer specifications, audiology and consumer publication sourcing, and community consensus — not personal clinical testing.</p>
        <p><strong>Important:</strong> The content on this site is for informational and educational purposes only. It is not a substitute for professional medical advice, diagnosis, or treatment. For questions about hearing loss, hearing aid selection, fitting, or your specific health needs, please consult a licensed audiologist or hearing healthcare professional.</p>
      </div>

      {persona.social?.pinterest && (
        <p><a href={persona.social.pinterest} rel="noopener noreferrer" target="_blank">Pinterest</a></p>
      )}
    </article>
  </div>
</BaseLayout>

<style>
  .about-page { max-width: 720px; margin: 0 auto; }
  .about-page__intro { display: flex; gap: 2rem; align-items: flex-start; margin-bottom: 2rem; flex-wrap: wrap; }
  .about-page__photo { border-radius: 50%; width: 160px; height: 160px; object-fit: cover; flex-shrink: 0; }
  .about-page__location { font-size: 0.9rem; color: var(--color-text-muted); margin-bottom: 0.75rem; }
  .about-page__body p { margin-bottom: 1rem; }
  .about-page__disclaimer { margin-top: 2.5rem; padding: 1.5rem; background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius); }
  .about-page__disclaimer h2 { font-size: 1.1rem; margin-bottom: 0.75rem; }
  .about-page__disclaimer p { font-size: 0.9rem; color: var(--color-text-muted); margin-bottom: 0.75rem; }
</style>
